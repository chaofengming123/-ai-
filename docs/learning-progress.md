# 学习进度

记录日期：2026-09-22。

## 当前阶段

Phase 7，第 26 课：向量实验接入 Qdrant，按账号与模型空间保存文字和向量，支持后续输入问题检索；尚未自动索引文档或进入 RAG 问答。

完整 Phase 0 的环境准备与知识点验收尚未全部完成，不将仓库初始化等同于阶段验收通过。

## 教学偏好

- 用户零基础，边逐步实现边简要说明发生了什么。
- 教学必须说明关键代码的作用、如何实现以及执行顺序，并写入每课讲义。
- 以能看见结果的小功能推进，不做逐标签练习。
- 避免频繁提问和冗长讲解；必要概念结合当前实现说明。
- 按完整手册推进，但不一次实现所有功能，不提前跳到后续阶段。
- 每次完成一批有意义的改动后，进行适当验证、Git 提交并推送一次，记录结果与下一步。
- 用户已提供远程地址：`git@github.com:chaofengming123/-ai-.git`。使用当前分支推送，不强制覆盖历史。

## 已完成

- 建立 `frontend/`、`backend/`、`docker/`、`docs/` 基础目录。
- 编写 README 与忽略规则。
- 将用户提供的完整手册原样保存为 `docs/learning-manual.md`。
- 记录学习方式与后续路线。

## 已知环境

以下来自前序环境检查，本次未重新安装或升级工具：

| 工具 | 已知状态 |
| --- | --- |
| Node.js | 26.4.0 |
| npm | 11.17.0 |
| Java | 17 |
| Git | 命令可用 |
| Docker | 命令可用，运行服务状态尚未确认 |
| Maven | 无全局安装；第 8 课已加入 Maven Wrapper 3.9.16 |

手册建议 JDK 21 与 Node.js LTS；当前环境与建议存在差异，进入相应工程阶段时再确认版本兼容性。第 8 课采用 Maven Wrapper，并选用支持 Java 17 的 Spring Boot 4.1.1。数据库及其他服务尚未验收。

## 第 1 课

- 已创建前端入口、侧边栏组件和知识库页面。
- 两条知识库数据集中在 `frontend/src/data/knowledgeBases.js`。
- 使用 `v-for` 渲染卡片，通过双大括号展示数据。
- 其他导航项明确标记为待开放；本节不加入业务操作。
- 运行方式与小练习见 `lesson-01.md`。
- 验证：`npm run build` 通过；浏览器确认显示两张知识库卡片及 3 / 10 份文档，其他导航项为禁用状态。
- 远程仓库已有 `main` 分支，本次课程提交推送至 `codex/initialize` 分支。

## 第 2 课

- 保留用户第 1 课的练习修改：“公司制度知识库”。
- 使用 `ref` 管理列表和输入，使用原生 dialog 打开创建表单。
- 使用 `v-model`、`@submit.prevent`、`trim`、`some` 和 `push` 完成输入、校验与新增。
- 讲义 `lesson-02.md` 逐段说明关键代码的作用与实现方式。
- 验证通过：生产构建、空白名称拦截、重复名称拦截、去除名称两端空格、创建后总数更新、再次打开清空输入、取消不新增、Esc 关闭和刷新恢复初始数据。

## 第 3 课

- 建立四个页面路由和根路径重定向，未知路径展示 404 页面。
- 用 RouterLink 实现导航，用 RouterView 显示页面，并同步高亮、顶部栏和浏览器标题。
- 用 KeepAlive 保留知识库页面的内存状态，切页不丢失新增记录；刷新仍重置。
- 文档与 AI 页面仅提供功能说明，未实现上传或问答。
- 关键代码的作用、实现方式与执行顺序见 `lesson-03.md`。
- 验证通过：生产构建、四个导航入口、标题和高亮、前进后退、切页保留新记录、直接访问及刷新文档路径、未知路径兜底。

## 第 4 课

- 拆出 `KnowledgeBaseCard.vue`，父页面用 Props 传入单条记录。
- 卡片发出 `view-detail` 事件并携带 id，父页面查找记录并打开只读详情。
- 保留新建、导航和 KeepAlive 行为，不引入持久化或后端。
- 关键代码作用、实现方式及完整数据流见 `lesson-04.md`。
- 验证：生产构建、两条初始记录和新建记录的详情、关闭与 Esc、焦点恢复及切页后详情操作。

## 第 5 课

- 用 ref 保存关键词，用 computed 从完整列表推导展示结果。
- 匹配名称、描述和分类，忽略两端空白和英文大小写。
- 支持显示筛选数量、清空和无结果提示；新增时保留当前筛选条件。
- 讲义 `lesson-05.md` 解释关键代码、执行顺序，以及 computed 缓存与 KeepAlive 的区别。
- 验证通过：生产构建、名称/描述/分类匹配、两端及全空白输入、英文大小写、无结果提示、清空恢复、搜索中新增自动匹配、筛选后查看详情和切页保留搜索词。

## 第 6 课

- Pinia setup store 管理列表 state、总数 getter 和创建 action。
- 两个页面通过 storeToRefs 读取共享状态；搜索词与弹窗仍留在页面。
- KeepAlive 继续缓存页面状态，Pinia 默认不持久化，刷新恢复初始记录。
- 关键代码及数据流见 `lesson-06.md`。
- 验证通过：构建、store 共享实例、空白/超长/重名校验、创建规范化与总数更新、搜索及详情、筛选不影响总数、刷新工作台恢复初始数量。

## 第 7 课

- api 模块提供等待 1 秒的 Promise，可显式模拟失败；未发送网络请求。
- Pinia 用 async/await 与 try/catch/finally 管理加载及错误状态。
- 首次加载前不显示误导性的 0 或空结果，重载失败保留旧记录。
- 模拟数据源保留本次新增记录，重新加载不丢失，整页刷新仍重置。
- 关键代码解释与小练习见 `lesson-07.md`。
- 验证通过：构建、首次加载反馈、模拟失败与重试、旧记录保留；store 检查覆盖首次失败恢复、重复加载拦截、加载中禁止创建、重新加载保留新建记录。

## 第 8 课

- Java 17 + Spring Boot 4.1.1，官方工程骨架，Maven Wrapper 管理构建工具。
- GET /api/health 返回 status 与 service；监听本机 8080。
- 讲义 `lesson-08.md` 说明启动类、Controller 注解、record、HTTP 与 JSON。
- 暂不接数据库；前端仍沿用模拟数据。
- 验证：Maven package 成功，3 项真实 HTTP 集成测试通过（GET 200/JSON、POST 405、未知路径 404）；打包 JAR 启动于 8080，实际 GET 返回预期 JSON。应用内浏览器拦截该 API 地址，未完成浏览器展示。

## 第 9 课

- GET 列表、GET 详情、POST 创建；后端内存数据重启后重置。
- Controller 接收请求，Service 处理规则与数据，构造器注入连接二者。
- 请求 DTO、响应 record、集中业务异常处理；400/404/409 错误响应。
- 7 项 HTTP 集成测试通过，包含第八课回归测试；讲义见 `lesson-09.md`。
- 前端仍使用模拟数据，下一课再连接。

## 第 10 课

- Axios GET/POST 替换前端模拟数据源，Vite 代理转发 /api 到 8080。
- 异步创建使用服务器记录与编号，提交中防重复，失败保留输入。
- 后端业务消息展示到表单；读取失败保留旧数据；不自动重发结果不确定的 POST。
- 讲义 `lesson-10.md` 说明真实请求链路、代理、await 和错误语义。
- 验证：前端构建与 store 回归测试通过；浏览器真实读取/重名/创建/刷新读回，保留后端原有记录。

## 第 11 课

- MySQL 8.4 开发容器绑定本机 3307，命名数据卷保存数据库。
- 密码由脚本生成到被忽略的 docker/.env；独立测试库与账户隔离测试数据。
- Flyway V1 创建表和初始记录；Mapper 替代 Service 内存 Map。
- 主键自增、名称唯一索引、数据库重名错误转为 409；讲义见 lesson-11.md。
- 已迁移旧后端 3 条记录并保持原编号；新建第 4 条记录后重启 JVM，查询结果完全一致。
- 8 项真实 MySQL 接口测试通过，含并发同名只创建一条；前端测试和构建通过，浏览器读取到 4 条持久化记录。

## 第 12 课

- PUT 修改名称与描述；DELETE 删除记录并返回 204，不存在返回 404。
- 共用保存 DTO 和字段校验；修改时事务与行锁保证读取更新衔接，重名返回 409。
- 卡片发出编辑和删除事件；表单草稿、删除确认、成功后更新共享列表和失败提示。
- 讲义 lesson-12.md 解释关键代码的作用、实现方法与完整执行顺序。
- 验证：9 项后端真实 MySQL 测试、2 项前端状态测试与构建通过；浏览器编辑/重名/刷新/删除取消通过，实际删除通过 API 验证。
- 清理本课临时记录后，开发库原有 4 条记录内容与编号保持一致。

## 第 13 课

- Flyway V2 新增 app_user 表，已有知识库表不做修改。
- 注册接口 POST /api/auth/register；用户名规范化、唯一索引、400 / 409 业务错误。
- BCrypt 密码哈希，响应仅有 id 和 username；独立 DTO、实体与响应对象。
- 创建账号页面、确认密码、提交中防重复、请求结束清空密码，不持久化密码。
- 讲义 lesson-13.md 解释关键代码、密码校验、各层数据转换及执行顺序。
- 验证：13 项后端测试、4 项前端测试及构建通过；浏览器入口、表单与空输入提示通过。
- 有效凭据注册在独立测试库验证；未代用户创建开发库账号。开发库 V2 已应用，本地服务已恢复。

## 第 14 课

- POST /api/auth/login 校验 BCrypt 密码并签发 15 分钟 JWT。
- Spring Security 保护 GET /api/auth/me，验证签名、时间、签发方，再按编号查询当前用户。
- JWT_SECRET 由脚本随机生成并保存在忽略文件，保留已有数据库及应用凭据。
- Pinia 保存内存登录状态、显示用户、支持核对和退出，丢弃退出前的迟到响应。
- 讲义 lesson-14.md 解释关键代码和请求顺序，明确刷新退出、无服务器撤销及业务接口尚未保护。
- 验证：17 项后端测试、6 项前端测试和构建通过；浏览器登录入口、空输入和不存在账号错误通过。

## 第 15 课

- 知识库列表、详情及新增修改删除都要求有效登录身份，认证检查账号仍存在。
- Axios 统一附带凭证，只处理当前登录周期的 401；不自动重试写请求。
- 路由守卫引导登录，成功返回允许的站内目标。
- 退出清空知识库 store 与 KeepAlive，读写动作丢弃旧周期响应。
- 讲义 lesson-15.md 说明关键代码、请求执行顺序与 IDEA 环境变量启动方式。
- 验证：18 项后端测试、9 项前端测试及构建通过；浏览器匿名跳转与运行中 API 的 401 已确认。
- 未修改开发库账号或知识库。当前登录用户共享业务数据，尚未实现角色授权。

## 第 16 课

- Flyway V3 增加角色字段和约束，现有与新注册账号默认 USER。
- 每次从数据库读取角色，使用 ROLE_ authority 与 hasRole 保护写请求，权限不足返回 JSON 403。
- 用户响应携带 role，前端显示角色与管理入口；业务 403 刷新身份，不退出、不自动重试。
- 提供本地角色设置脚本，默认仅预览，--apply 修改明确指定的账号；没有自动提权开发账号。
- 讲义 lesson-16.md 解释认证与授权、关键代码作用及执行顺序。
- 验证：20 项后端测试、11 项前端测试与生产构建通过，角色脚本参数帮助检查通过。

## 第 17 课

- V4 创建角色、权限及两张关联表，迁移保留已有角色，移除旧 role 字段。
- 注册事务包含账号及默认角色分配；响应改为 roles 与 permissions 数组。
- 按具体权限授权，新增 EDITOR，多角色权限取并集，数据库变更作用于下一次请求。
- 前端分开显示新建、编辑、删除入口；核对身份发现权限变化时重置业务缓存。
- 本地脚本支持一个或多个角色，默认预览，明确替换全部角色。
- lesson-17.md 说明关键代码作用、实现、执行顺序及操作练习。
- 验证：22 项后端测试、11 项前端测试与构建通过。开发库 V4 已应用，迁移前后账号、密码、原角色及知识库校验一致；运行中匿名知识库请求返回 401。
- 修复角色脚本在 MySQL 8.4 下混用字符排序规则导致的预览失败，并在讲义补充数据库与 8080 端口排查方法。

## 第 18 课

- V5 新增 document 元数据表、知识库外键和文档查看／上传权限，保留原有账号及角色分配。
- POST /api/documents 接收 multipart 文件表单；GET /api/documents 按知识库列出文档。
- 单文件最多 1 MB，支持 UTF-8 TXT / Markdown，校验空文件、路径文件名及非法编码。
- 原文件按 UUID 保存在本地；数据库明确回滚时清理新文件，文档数量改为统计真实记录。
- 前端按权限显示上传入口，处理错误、重复提交、切换知识库及会话变化后的迟到响应。
- 含文档的知识库暂不允许删除；本课不实现下载、解析、向量化或 AI 问答。
- lesson-18.md 解释关键代码作用、实现、执行顺序及本地存储过渡到 MinIO 的安排。
- 验证：27 项后端测试、13 项前端测试及生产构建通过；匿名文档页面跳转登录。新增存储失败场景通过完整 Service 验证。
- 开发库 V5 已应用，临时后端健康检查 200、匿名文档接口 401；验证后停止临时进程，8080 保留给 IDEA 启动。

## 第 19 课

- 新增本机 MinIO 容器与持久卷，固定官方安全发布源码构建；初始化脚本追加随机凭据并保留已有配置。
- V6 为文档增加存储类型和桶名，旧记录默认为 LOCAL，新上传默认使用 MINIO。
- DocumentStorage 统一分发本地与对象存储操作；MinIO 桶保持私有，对象键禁止覆盖。
- 增加受 document:read 保护的原文件下载，Vue 通过带 JWT 的 Blob 请求保存附件。
- 迁移工具默认预览；正式迁移读回校验后更新定位字段，保留本地副本，可安全重跑。
- 开发库 V6 已应用，1 份旧文档迁移并校验成功，本地备份保留；没有停止用户在 8080 的后端，需在 IDEA 重启新版并加载 MinIO 凭据。
- 验证：32 项后端测试、13 项前端测试及构建通过；补充的 MinIO 防覆盖验证通过。真实测试覆盖上传下载、匿名对象访问拒绝、事务补偿、缺失源文件和迁移校验失败。
- lesson-19.md 解释 bucket、object key、SDK、数据库定位、事务补偿和下载；说明社区归档与本地构建方式。

## 第 20 课

- 上传类型扩展为 TXT / Markdown / PDF / DOCX，沿用 1 MB 上限与现有存储、权限和下载流程。
- 独立 DocumentFormatValidator 校验格式；PDFBox 3.0.8 检查 PDF 可读性、加密与页数。
- DOCX 检查 ZIP、必要 XML、主部件关系、CRC、路径、宏及解压限制；XML 禁用外部资源和 DOCTYPE。
- 校验发生在写入 MinIO / MySQL 之前，成功文件仍保存原始字节，不抽取正文。
- lesson-20.md 解释后缀、MIME、内容校验与关键代码，并说明本课的结构检查边界。
- 验证：40 项后端测试、13 项前端测试及构建通过，包括真实 HTTP 与 MinIO 的 PDF / DOCX 字节往返、非法格式不落库。
- 无新增数据库迁移；保留开发文档与现有 8080 后端进程，需要用户重启新版后端。

## 第 21 课

- 增加 ChatController / ChatService / LlmClient，使用非流式 Chat Completions；最新默认配置为智谱 GLM-4.7-Flash，关闭思考模式。
- V7 增加 chat:send 权限，USER、EDITOR、ADMIN 可使用；JWT 与模型 API Key 分离，Key 只保存在后端。
- 校验角色顺序与消息长度，拼接后端 system 指令；同用户单请求、进程最多四并发，超时和失败不自动重试。
- Vue 展示文本回复、保留最近五轮请求上下文，支持新对话、错误和截断提示，丢弃旧会话的迟到回复。
- lesson-21.md 解释关键代码的作用、实现与执行顺序，附智谱本地配置、IDEA 启动和验证步骤，以及旧模型环境变量覆盖默认值的排查说明。
- 验证：40 项原有后端测试及 9 项新增聊天测试通过，DeepSeek 参数调整后重跑 9 项聊天测试通过；17 项前端测试及构建通过。模型测试全部使用本地模拟上游，没有调用真实服务商。
- 开发库 V7 已应用，随机端口后端健康检查 200、匿名 Chat 请求 401，临时进程已停止。现有 8080 后端未停止；用户需填入本地 API Key、重启新版后端并重新登录，完成真实模型验收。
- 后续按用户要求将默认地址与模型切换为智谱 GLM-4.7-Flash，更新示例及讲义；9 项聊天测试与后端打包通过。本地 docker/.env 尚无 LLM 配置，需用户填入智谱 API Key；未调用真实模型。

## 第 22 课

- 新增受 chat:send 保护的 POST /api/chat/stream，与非流式接口共用消息校验、system 指令和并发名额。
- LlmClient 请求 stream=true，解析上游 SSE 并立即转发 delta；使用流字节上限、整体截止计时器和资源关闭保护。
- Controller 发送 delta / done / error，区分响应提交前的 JSON 错误与提交后的 SSE 错误。
- Vue 使用 fetch + TextDecoder 增量解析，支持停止接收、中文分块、不完整回复提示和旧会话片段丢弃；仅成功完成的问答进入上下文。
- lesson-22.md 解释 SSE 帧、flush、流解析、JWT、取消与超时的关键代码及执行顺序，保留第二十一课非流式接口用于对照。
- 验证：58 项后端测试、24 项前端测试及生产构建通过；本地模拟上游在结束前暂停，客户端仍能收到第一段。验证了断流、超时、权限、UTF-8 分块和上下文隔离，没有调用真实智谱服务。
- 无新增数据库迁移，现有后端进程保持运行；需重启新版后端并用本地智谱配置验收真实流式回复。
- 构建期间发现依赖仍有 iCloud dataless 文件，逐个请求下载后测试与构建恢复正常。

## 第 23 课

- 新增 GET /api/documents/{id}/text，要求 document:read，复用文档下载链读取原文件并核对大小，返回 no-store 正文预览。
- DocumentTextExtractor 提取 UTF-8 文本、PDF 文本层及 DOCX 主文档段落／表格；DOCX 复用现有受限 ZIP 与安全 XML 校验。
- 预览最多 40000 UTF-16 字符，PDF 限前 20 页，进程最多两个并发解析；截断、空文本和格式限制均明确说明。
- 前端增加查看正文、关闭、加载和错误状态；切换文件／知识库或会话后丢弃旧响应，正文仅作为文本显示。
- lesson-23.md 解释校验与解析、字节与文字、PDFTextStripper、DOCX 节点遍历、受限 Writer 和完整请求顺序。
- 验证：第一轮 64 项后端回归测试通过；补充 MinIO 正文预览及繁忙状态后，25 项文档相关测试与打包通过；26 项前端测试及生产构建通过。原文件字节、UPLOADED 状态保持不变。
- 无数据库迁移，无模型调用，不持久保存正文；需重启新版后端，在文档管理中点击“查看正文”。

## 第 24 课

- 新增 TextChunker，按 UTF-16 字符窗口分块，优先段落、换行、标点与空格边界，返回原文范围和实际重叠。
- 新增受 document:read 保护的 GET /api/documents/{id}/chunks，校验大小与重叠参数，复用受限正文提取，保留 sourceTruncated 与格式说明。
- 前端独立分块预览组件支持重新计算、重叠高亮、逐块展开；取消并丢弃旧请求，参数单位明确为字符。
- lesson-24.md 解释窗口、偏移、边界、重叠、代理对与请求链，附可上传的分块实验材料。
- 验证：71 项后端测试、28 项前端测试及生产构建通过；验证原文还原、Unicode 边界、权限、无效参数、截断标记与旧结果隔离。
- 无数据库迁移，不改写原文件，不调用模型；需重启后端，在文档管理中点击“分块预览”。

## 第 25 课

- 新增 EmbeddingController / Service / Client 与向量实验页面，复用 chat:send 权限。
- 默认硅基流动 BAAI/bge-m3，独立地址、模型和密钥；不保留本地模型配置，无自动切换或重试。
- 批量生成问题与候选向量，按 index 恢复顺序，校验向量并用全部维度计算余弦相似度；页面仅预览前八个坐标。
- lesson-25.md 解释关键代码作用、实现与请求顺序，附终端和 IDEA 配置、实验与收费模型迁移说明。
- 自动测试使用模拟上游，真实模型尚需用户配置密钥验收；无数据库迁移和向量持久化。
- 验证：79 项后端测试、30 项前端测试及生产构建通过；已有 8080 后端需重启后使用本课功能。

## 第 26 课

- Compose 增加固定版本 Qdrant v1.19.1、本机端口与独立持久卷；Embedding 继续用硅基流动。
- 新增 VectorLabController / Service / QdrantClient 与保存后检索组件，复用 chat:send 权限。
- 账号 ID 来自 JWT，服务地址与模型摘要隔离向量空间；按文字生成稳定 ID，upsert 等待完成，冲突时不删库重建。
- 保存批量向量化候选，搜索仅向量化问题并返回前三条；页面支持状态读取和旧请求隔离。
- lesson-26.md 解释关键代码、Collection / Point / Payload、持久卷、模型迁移边界和完整请求顺序。
- 测试使用真实 Qdrant 与模拟模型；没有调用真实硅基流动、没有新增 MySQL 迁移。已有 8080 后端需用户重启。
- 验证：83 项后端测试、32 项前端测试及生产构建通过；独立测试集合在 Qdrant 重启后仍可读取，验收后已删除测试集合。

## 下一步

接入完整文档索引，记录文档归属、处理状态和索引版本，再实现检索问答；正式入库不能将受限预览当成全文。

## 后续路线

依照 `learning-manual.md`：Spring Boot API → MySQL 知识库 CRUD → 登录与权限 → 文档上传 → LLM Chat → RAG 文档处理与检索 → 高级检索 → Redis 与异步处理 → Docker 部署 → 测试、可观测性、评估及 AI 安全。

## 初始化记录

首次提交仅包含初始化文档、忽略规则和目录占位文件。使用已有 Git 身份，不创建 GitHub 远程或推送。初始化提交为 `8db385e`。此后按用户新要求，每次更新均提交并推送；实际结果以 Git 历史和任务报告为准。
