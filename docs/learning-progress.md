# 学习进度

记录日期：2026-09-20。

## 当前阶段

Phase 1，第 2 课：已实现 Mock Data 新建知识库表单、响应式列表和输入校验。尚未接入后端、数据库或 AI；新增数据刷新后不保留。

完整 Phase 0 的环境准备与知识点验收尚未全部完成，不将仓库初始化等同于阶段验收通过。

## 教学偏好

- 用户零基础，边逐步实现边简要说明发生了什么。
- 教学必须说明关键代码的作用、如何实现以及执行顺序，并写入每课讲义。
- 以能看见结果的小功能推进，不做逐标签练习。
- 避免频繁提问和冗长讲解；必要概念结合当前实现说明。
- 按完整手册推进，但不一次实现所有功能，不提前跳到后续阶段。
- 每次完成一批有意义的改动后，进行适当验证、Git 提交并推送一次，记录结果与下一步。
- 用户已提供远程地址：`git@github.com:chaofengming123/-ai-.git`。使用当前分支推送，不强制覆盖历史。

## 已完成

- 建立 `frontend/`、`backend/`、`docker/`、`docs/` 基础目录。
- 编写 README 与忽略规则。
- 将用户提供的完整手册原样保存为 `docs/learning-manual.md`。
- 记录学习方式与后续路线。

## 已知环境

以下来自前序环境检查，本次未重新安装或升级工具：

| 工具 | 已知状态 |
| --- | --- |
| Node.js | 26.4.0 |
| npm | 11.17.0 |
| Java | 17 |
| Git | 命令可用 |
| Docker | 命令可用，运行服务状态尚未确认 |
| Maven | 未找到 |

手册建议 JDK 21 与 Node.js LTS；当前环境与建议存在差异，进入相应工程阶段时再确认版本兼容性。后端开始前补齐 Maven 或采用 Maven Wrapper。数据库及其他服务尚未验收。

## 第 1 课

- 已创建前端入口、侧边栏组件和知识库页面。
- 两条知识库数据集中在 `frontend/src/data/knowledgeBases.js`。
- 使用 `v-for` 渲染卡片，通过双大括号展示数据。
- 其他导航项明确标记为待开放；本节不加入业务操作。
- 运行方式与小练习见 `lesson-01.md`。
- 验证：`npm run build` 通过；浏览器确认显示两张知识库卡片及 3 / 10 份文档，其他导航项为禁用状态。
- 远程仓库已有 `main` 分支，本次课程提交推送至 `codex/initialize` 分支。

## 第 2 课

- 保留用户第 1 课的练习修改：“公司制度知识库”。
- 使用 `ref` 管理列表和输入，使用原生 dialog 打开创建表单。
- 使用 `v-model`、`@submit.prevent`、`trim`、`some` 和 `push` 完成输入、校验与新增。
- 讲义 `lesson-02.md` 逐段说明关键代码的作用与实现方式。
- 验证通过：生产构建、空白名称拦截、重复名称拦截、去除名称两端空格、创建后总数更新、再次打开清空输入、取消不新增、Esc 关闭和刷新恢复初始数据。

## 下一步

加入 Vue Router，让侧边导航真正切换页面，理解 URL 与页面组件的映射。暂不连接数据库，不实现 AI 或 RAG。

## 后续路线

依照 `learning-manual.md`：Spring Boot API → MySQL 知识库 CRUD → 登录与权限 → 文档上传 → LLM Chat → RAG 文档处理与检索 → 高级检索 → Redis 与异步处理 → Docker 部署 → 测试、可观测性、评估及 AI 安全。

## 初始化记录

首次提交仅包含初始化文档、忽略规则和目录占位文件。使用已有 Git 身份，不创建 GitHub 远程或推送。初始化提交为 `8db385e`。此后按用户新要求，每次更新均提交并推送；实际结果以 Git 历史和任务报告为准。
