# 补充到 docker/.env，保留已有数据库、Embedding 与 GLM 配置。
RERANK_ENDPOINT='https://api.siliconflow.cn/v1/rerank'
RERANK_MODEL='BAAI/bge-reranker-v2-m3'
RERANK_API_KEY=''
# 填硅基流动 API Key，可使用同一账号的 Embedding Key；不能填智谱 Key。
# 使用无 Pro/ 前缀的模型；独立配置，未来更换兼容服务时不会带走其他服务的密钥。
# 只在本机填写真实密钥，修改后重启后端。不要提交 docker/.env。
